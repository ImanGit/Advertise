@model IEnumerable<Advertise.ViewModel.Models.Categories.Category.CategoryViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
   iman
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CreatorUserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CreatorUserName)
        </td>
      
    </tr>
}

</table>
